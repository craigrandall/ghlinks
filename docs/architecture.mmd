flowchart TD
    subgraph Input
        A["Input file\n(list of URLs)"]
    end

    subgraph Classification
        B["classify.rs\nURL to LinkKind\n(repo root/file, gist, pages,\nuser/org profile, unsupported, unknown)"]
    end

    subgraph GitHub
        C["github.rs\nGraphQL-first client\nrepo/gist/pages metadata"]
    end

    subgraph Discovery
        D["discovery.rs\nHacker News (Algolia)\nexternal mentions"]
    end

    subgraph Retry
        R["retry.rs\nshared retry/backoff policy\n(pure functions, unit-tested)"]
    end

    subgraph Model
        E["model.rs\nLinkRecord, RepoData, GistData,\nReleaseEntry, ExternalMention,\nExternalDiscovery, RunSummary, Report"]
    end

    subgraph Orchestration
        F["main.rs\nCLI, concurrency,\nJSON output"]
    end

    A --> B
    B --> C
    B --> D
    R -.-> C
    R -.-> D
    C --> E
    D --> E
    E --> F
